import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dysgrafia-detection-app',
  webDir: 'dist'
};

export default config;
